twitter-bulk-users-lookup
=========================

Python script built on ScraperWiki using the Twitter users/lookup API that accepts a list of Twitter user names or IDs and returns a simple sqlite database.